apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: IOT ML Training Job
spec:
  schedule: "0 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: ml-training-container
              image: bash
          restartPolicy: OnFailure